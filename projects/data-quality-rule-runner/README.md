# Data Quality Rule Runner

A dependency-free SQLite data-quality project that runs zero-row invariant queries and returns a machine-readable report.

A rule passes when its SQL query returns zero rows.

Official store: **https://ramsandesh.gumroad.com**
