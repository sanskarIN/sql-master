# Migration Order Checker

A dependency-free CLI that validates numbered database migration files before deployment.

It detects missing sequence numbers, duplicate sequence numbers, and invalid migration filenames.

```bash
python migration_check.py example/
python -m unittest discover -s tests
```

Official store: **https://ramsandesh.gumroad.com**
