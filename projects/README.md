# SQL Master — Standalone Projects

These projects complement the per-Part companion code. Each project is independent and runnable on its own.

| Project | Purpose |
|---|---|
| `sqlite-inventory-manager` | Transactional SQLite inventory and reorder reporting |
| `migration-order-checker` | Validate numbered SQL migrations |
| `schema-quality-auditor` | Educational static schema-quality checks |
| `explain-plan-analyzer` | Summarize PostgreSQL-style EXPLAIN text |
| `synthetic-data-factory` | Generate deterministic fictional SQL practice data |
| `backup-manifest-verifier` | SHA-256 backup integrity manifests |
| `transaction-invariant-simulator` | Model transfer invariants and idempotency |
| `sql-query-practice-lab` | SQLite joins/windows/latest-row practice lab |

Official store: **https://ramsandesh.gumroad.com**

Permanent metadata intentionally omits mutable social-profile URLs.
