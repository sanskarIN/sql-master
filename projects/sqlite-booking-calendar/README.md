# SQLite Booking Calendar

A small SQLite/Python project for practicing booking intervals and overlap checks.

It demonstrates application-level overlap validation for a single SQLite database and is intentionally simple.

```bash
python -m unittest discover -s tests
```

Official store: **https://ramsandesh.gumroad.com**
