# SQL Query Practice Lab

A dependency-free SQLite lab with a small commerce schema and automated answer checks.

Topics include joins, anti-joins, grouping, window functions, latest-row-per-group, and running totals.

```bash
python -m unittest discover -s tests
```

Official store: **https://ramsandesh.gumroad.com**
